// GC-00: no examination available.
// Expected behavior: decision not evaluated or missing-data explanation.

case(gc00).

unavailable(gc00, echo).
unavailable(gc00, cmr).
unavailable(gc00, ct_pet).
unavailable(gc00, pet).
